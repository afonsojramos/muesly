// Prerender the entire site to static files (no server logic). Per-route
// `prerender = false` would opt a single route out, if ever needed.
export const prerender = true;
export const trailingSlash = 'never';
