---
import { Icon } from 'astro-icon/components';
import Section from '../ui/Section.astro';

const stats = [
	{ figure: '0', label: 'accounts required to start' },
	{ figure: '2', label: 'on-device transcription engines' },
	{ figure: '1', label: 'local database you control' }
];
---

<Section id="privacy" class="py-16 md:py-20">
	<div class="relative overflow-hidden rounded-[32px] bg-card p-8 shadow-card md:p-14" data-reveal>
		<div class="absolute inset-x-0 top-0 h-1 bg-gradient-to-r from-transparent via-accent to-transparent opacity-70"></div>
		<div class="mx-auto max-w-2xl text-center">
			<span
				class="inline-flex h-11 w-11 items-center justify-center rounded-2xl bg-accent/10 text-accent"
			>
				<Icon name="lucide:lock" class="h-5 w-5" aria-hidden="true" />
			</span>
			<h2 class="mt-4 font-display text-3xl font-semibold tracking-tight md:text-4xl">
				Your conversations stay yours
			</h2>
			<p class="mt-3 leading-relaxed text-muted-foreground">
				Meeting audio, transcripts, and summaries are processed and stored on your device. muesly is
				built for people whose conversations can't live on someone else's servers.
			</p>
			<p class="mt-2 text-sm text-muted-foreground">
				Optional anonymized usage analytics is off until you opt in, and cloud LLM summaries
				are used only if you configure a provider.
				<a class="text-accent underline underline-offset-2" href="/privacy">Read the privacy policy.</a>
			</p>
		</div>

		<dl class="mx-auto mt-10 grid max-w-3xl gap-6 text-center sm:grid-cols-3">
			{
				stats.map((stat) => (
					<div>
						<dt class="font-display text-3xl font-semibold text-foreground tabular-nums">
							{stat.figure}
						</dt>
						<dd class="mt-1 text-sm text-muted-foreground">{stat.label}</dd>
					</div>
				))
			}
		</dl>
	</div>
</Section>
