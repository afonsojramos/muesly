# cargo audit does NOT scan git dependencies (ffmpeg-sidecar, cidre, silero).
# Those rev pins should be reviewed manually on a periodic basis to pick up
# any upstream fixes that are not yet tracked by RUSTSEC advisories.

name: "Security Audit"

on:
  pull_request:
    paths:
      - "**/Cargo.toml"
      - "**/Cargo.lock"
      - ".cargo/audit.toml"
      - ".github/workflows/audit.yml"
  push:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1" # Mondays: catch advisories filed after a PR merged

# audit-check reports results by creating a Check Run, which needs an explicit
# grant when the default GITHUB_TOKEN is read-only.
permissions:
  contents: read
  checks: write

jobs:
  cargo-audit:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5
      - uses: rustsec/audit-check@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
